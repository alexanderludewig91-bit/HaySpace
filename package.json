{
  "name": "modern-arcade-space-shooter",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "fix-base": "node scripts/fix-base-path.js",
    "build:pages": "vite build && npm run fix-base",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}

